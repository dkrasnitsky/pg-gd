# <Название фичи>

**Статус:** готово / следующий приоритет / не начато
**Тип события в календаре:** <тип, либо "нет — фича не создаётся через календарь">

## 1. Где что настраивается

| Где | Что именно настраивается здесь |
|---|---|
| Инструмент («Конструктор ...» в разделе Инструменты) | |
| График ивентов (окно создания/редактирования события) | |

## 2. Google Sheets — точки записи

Может быть несколько независимых точек: собственная таблица фичи и/или общие таблицы, которые читают другие системы (например EventCenterConfig).

### Точка A: собственная таблица фичи
**Таблица:** <ссылка/ID>
**Паттерн:** дублирование шаблонных листов / добавление строк в существующий лист / другое

- **<лист>** — <дублируется в `<имя>` / редактируется на месте>
  - Поля/колонки: `...`
- **<лист>** —
  - Поля/колонки: `...`

### Точка B: EventCenterConfig (общая таблица)
**Таблица:** <ссылка/ID>
**Лист:** `<имя листа под эту фичу>`
**Поля новой строки:** `...`

*(если у фичи есть ещё точки записи — добавить Точку C и т.д.)*

## 3. Test / Prod режим

- Где в UI переключатель тест/прод (если есть)
- Тестовые адреса/листы: `...`
- Прод адреса/листы: `...`

## 4. Ограничения

- Структуру менять нельзя — Unity Config Manager ожидает: `...`
- Прочее: `...`

## 5. Открытые вопросы

-
